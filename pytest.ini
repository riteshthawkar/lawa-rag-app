[pytest]
addopts = -ra
testpaths = tests
markers =
    live: exercises real external providers and should only run in gated environments
    deployed: exercises a deployed environment and should only run after deployment
